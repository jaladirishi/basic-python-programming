# product of all prime numbers from m, n
def is_prime(x):
    if x < 2:
        return False
    for i in range(2, int(x**0.5)+1):
        if x % i == 0:
            return False
    return True

def product_of_prime_integers(m, n):
    if m > n:
        return -1
    product = 1
    found = False
    for i in range(m, n+1):
        if is_prime(i):
            product *= i
            found = True
    if found:
        return product
    else:
        return "No prime numbers in range."

m = int(input("enter m: "))
n = int(input("enter n: "))
result = product_of_prime_integers(m, n)
print(result)
